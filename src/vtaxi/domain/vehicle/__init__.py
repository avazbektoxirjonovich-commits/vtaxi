"""Vehicle context: a driver's registered vehicle, its documents and
photos. Split out from Driver Verification once `infrastructure/database/
models/vehicle.py` (Step 5.3) established it as its own bounded context
with four models of its own -- this folder was never created for it at
Step 2, added now that framework-free Vehicle-domain content (exceptions)
exists.
"""
