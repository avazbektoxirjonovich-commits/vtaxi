"""SQLAlchemy 2.0 foundation: `Base`, mixins, enums, constants and types (this
step). Async engine, session factory, and repository implementations are a
later step -- nothing here talks to a database yet.
"""
